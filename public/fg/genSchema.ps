1.Gen Schema spec

 if container has any one child field as mandatory , container's madf should be Yes
 if  all child's mndf is no , container's mndf should be No
 if container is deleted , all child fields should be deleted 
 if conatiner dataType and html type should be 'CONTAIER'
 if 